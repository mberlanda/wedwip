jQuery(document).ready(function() {
  $.backstretch([
      "<%= asset_path('stockholm.jpg') %>",
      "<%= asset_path('stockholm.jpg') %>"
      ], {
        fade: 1000,
        duration: 8000
  	}
  	);
});